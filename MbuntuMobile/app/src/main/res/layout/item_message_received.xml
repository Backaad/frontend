<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingVertical="4dp"
    android:paddingHorizontal="8dp">

    <!-- ... ImageView pour l'avatar ... -->

    <!-- NOUVEAU : TextView pour le nom de l'expéditeur -->
    <TextView
        android:id="@+id/textViewSenderName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:textSize="12sp"
        android:textStyle="bold"
        android:textColor="?attr/colorPrimary"
        app:layout_constraintStart_toEndOf="@+id/imageViewSenderAvatar"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Bob" />

    <!-- La bulle du message est maintenant contrainte sous le nom -->
    <TextView
        android:id="@+id/textViewMessageContent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/background_message_received"
        android:paddingHorizontal="16dp"
        android:paddingVertical="12dp"
        android:textColor="@color/mbuntu_text_primary"
        android:maxWidth="280dp"
        app:layout_constraintStart_toStartOf="@+id/textViewSenderName"
        app:layout_constraintTop_toBottomOf="@+id/textViewSenderName"
        tools:text="Ceci est un exemple de message reçu." />

    <!-- L'heure est maintenant contrainte sous la bulle -->
    <TextView
        android:id="@+id/textViewMessageTimestamp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textSize="10sp"
        android:textColor="@color/mbuntu_text_secondary"
        app:layout_constraintStart_toStartOf="@+id/textViewMessageContent"
        app:layout_constraintTop_toBottomOf="@+id/textViewMessageContent"
        tools:text="14:35" />

</androidx.constraintlayout.widget.ConstraintLayout>