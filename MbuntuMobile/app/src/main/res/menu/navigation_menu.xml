<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_conversations"
            android:icon="@android:drawable/ic_dialog_email"
            android:title="Conversations" />

        <!-- NOUVEL ITEM AJOUTÉ POUR LE PROFIL -->
        <item
            android:id="@+id/nav_profile"
            android:icon="@android:drawable/ic_menu_myplaces"
            android:title="Mon Profil" />

        <item
            android:id="@+id/nav_settings"
            android:icon="@android:drawable/ic_menu_manage"
            android:title="Paramètres" />
    </group>

    <!-- Groupe séparé pour les actions comme la déconnexion -->
    <item android:title="Compte">
        <menu>
            <item
                android:id="@+id/nav_logout"
                android:icon="@android:drawable/ic_menu_close_clear_cancel"
                android:title="Déconnexion" />
        </menu>
    </item>

</menu>